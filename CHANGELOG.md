# Changelog

## 0.1.0

Initial native macOS menu bar release: read-only live quota query, separate buckets, actual-duration windows, explicit offline log snapshots, stale/error states, manual refresh, optional login item, Universal packaging, CLI and regression tests.
